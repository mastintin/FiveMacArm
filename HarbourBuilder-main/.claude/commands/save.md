Review all changes made in this session (use git diff and git status). Then:

1. Update ChangeLog.txt: add a new dated entry at the top summarizing what was done. Use bullet points, be specific but concise. Follow the existing format in the file.
2. Stage all modified and new source files (do NOT stage binary/object files like \*.o, compiled executables, or .DS\_Store).
3. Create a git commit with a descriptive one-line summary message. Include details in the commit body if there are many changes.

4\. push to remote.

